apiVersion: thinkube.io/v1
kind: ThinkubeDeployment

spec:
  deployment:
    type: component
    name: text-embeddings
    replicas: 0

  containers:
    - name: inference
      build: .
      port: 7860
      size: xlarge
      gpu:
        count: 1
        memory: "8Gi"
      health: /health
      test:
        enabled: false

  routes:
    - path: /
      to: inference
