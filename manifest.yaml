apiVersion: thinkube.io/v1
kind: TemplateManifest
metadata:
  name: tkt-text-embeddings
  title: Text Embeddings Server (TEI)
  description: High-performance text embeddings using Hugging Face TEI — model managed by LLM Gateway
  version: 1.0.0
  author: Alejandro Martinez Corria
  tags: ["ai", "embeddings", "rag", "text-embeddings", "tei", "huggingface"]

parameters: []

secrets:
  - name: HF_TOKEN
    description: Hugging Face API token — required at runtime for gated models
    required: false
