# Text Embeddings - Infinity backend with Gradio UI

# Infinity embedding server (torch backend, flash attention)
infinity-emb[all]>=0.0.70

# Requests - For HTTP requests (Keycloak authentication, Infinity API)
requests>=2.28.0

# Gradio - Web UI (>=5.47.2 required for huggingface_hub 1.0 compatibility)
gradio>=5.47.2

# NumPy - For embedding vector operations
numpy>=1.24.0
