# Text Embeddings - TEI backend with Gradio UI

# Requests - For HTTP requests (Keycloak authentication, TEI API)
requests>=2.28.0

# Gradio - Web UI (>=5.47.2 required for huggingface_hub 1.0 compatibility)
gradio>=5.47.2

# NumPy - For embedding vector operations
numpy>=1.24.0
